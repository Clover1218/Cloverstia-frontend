# 2026-09-22

## 落地页增加备案信息（方案 A：底部居中细条，ICP + 公安）
- 在 `src/pages/index.astro` 增加固定底部居中的备案 footer：
  - frontmatter 顶部新增常量 `ICP_NO / ICP_URL / PS_NO / PS_URL`（带清晰占位符，待用户填真实编号）。
  - HTML：`<footer class="beian">` 内含 ICP 链接（→ beian.miit.gov.cn）+ 公安链接（→ www.beian.gov.cn，内联盾牌 SVG 图标），中间用 `·` 分隔。
  - CSS：半透明白字 + 极淡毛玻璃底、12px、底部居中 fixed、`prefers-reduced-motion` 适配、≤360px 竖排。
- `astro check` 0 errors。完整 `pnpm build`/dev 在本沙箱仍因 safe-delete 钩子与 localhost 隔离无法跑（环境限制，非代码问题）。

## 备案信息改为引用 config（同日下午）
- 用户提示备案号已写在 `src/config.ts` 的 `config.site` 下：`icpRecord`、`policeRecord`、`policeRecordUrl`（公安指向 beian.mps.gov.cn）。
- 落地页 `src/pages/index.astro` 改为 `import config from "@/config"`，备案常量直接取 `config.site.icpRecord / policeRecord / policeRecordUrl`；ICP 官方链接 beian.miit.gov.cn 项目未提供，保留为常量。
- `astro check` 0 errors。

## 修复备案条不居中（同日晚）
- 原因：`.beian` 同时有 `fade-in` 动画和 `transform: translateX(-50%)` 定位，动画 fill-mode 覆盖 transform，导致条偏右、被挤成两行。
- 修复：改为 `left:0; right:0; margin-inline:auto; width:fit-content` 居中，不再依赖 transform，与淡入动画兼容。
- `astro check` 0 errors。
